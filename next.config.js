/** @type {import('next').NextConfig} */
const nextConfig = {
	// Vercel will automatically detect Next.js projects
	// No additional configuration needed for basic setup
};

module.exports = nextConfig;

